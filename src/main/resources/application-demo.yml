spring:
  profiles:
    active: @spring.profiles.active@

server:
  port: 5000

app:
  datasource:
    jdbc-url: jdbc:postgresql://<yourEBurl>
    username: <yourusername>
    password: <yourpassword>
    pool-size: 30